#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @x: the int value to be checked
 * Return: the absolute value of the int
 */

int _abs(int x)
{
	if (x >= 0)
	{
		return (x);
	}
	return (-x);
}

